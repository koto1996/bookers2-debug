
  <thead>
    <tr>
      <th>今週の投稿</th>
      <th>先週の投稿</th>
      <th>前週比</th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @this_weeek_book.count %></td>
      <td><%= @last_week_book.count %></td>
      <td>
        <% if @last_weeek_book.count == 0 %>
        先週の投稿はありません
        <% else %>
        <% @the_week_before = @this_week_book.count / @last_week_book.count.to_f %>
        <%= (@the_dweek_before) * 100).round %>
        <% end %>
      </td>
    </tr>
  </tbody>
</table>